<?xml version="1.0" encoding="utf-8"?>
<com.loopeer.compatinset.InsetLinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="@bool/fits_system_windows"
    android:orientation="vertical">

    <include layout="@layout/view_toolbar"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <android.support.design.widget.CustomTabLayout
            android:id="@+id/drama_select_tab_select"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/color_white"
            app:tabBackground="@color/window_background_white"
            app:tabGravity="fill"
            app:tabIndicatorColor="@android:color/black"
            app:tabMinWidth="72dp"
            app:tabMode="scrollable"
            app:tabSelectedTextColor="@android:color/black"
            app:tabTextAppearance="@style/TabLayoutTextAppearance"/>

        <ImageView
            android:id="@+id/blurLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:src="@drawable/shape_tab_slide"/>

    </RelativeLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/drama_select_view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</com.loopeer.compatinset.InsetLinearLayout>